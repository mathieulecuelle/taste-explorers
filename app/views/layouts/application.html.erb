<!DOCTYPE html>
<html>
  <head>
    <title>Taste Explorers - Plongez dans les saveurs locales et du monde autour de chez vous !</title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= favicon_link_tag asset_path('eat.png') %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/flatpickr/dist/flatpickr.min.css">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <link rel="manifest" href="<%= asset_path 'manifest.json' %>">
    <%= render 'shared/ios_pwa_links' %>
  </head>

  <body>
    <div class="container-taste">
      <%= yield %>
    </div>
      <% if controller_name == 'meals' && action_name == 'show' %>
      <%= render "shared/navbar-resa", meal: @meal %>
      <% else %>
      <%= render "shared/navbar"%>
      <% end %>

  </body>
</html>
